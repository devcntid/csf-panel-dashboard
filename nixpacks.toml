# Nixpacks configuration untuk Railway
# Install Python dan build tools untuk compile native dependencies

[phases.setup]
nixPkgs = ["python3", "gcc", "make"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[start]
cmd = "node server.js"
